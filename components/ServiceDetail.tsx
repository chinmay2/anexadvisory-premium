import Link from "next/link";
export default function ServiceDetail({eyebrow,title,intro,sections}:{eyebrow:string;title:string;intro:string;sections:{n:string;t:string;items:string[]}[]}){return <main><section className="detail-hero"><div className="container"><div className="eyebrow">{eyebrow}</div><h1>{title}</h1><p className="hero-copy">{intro}</p></div></section><section className="section"><div className="container"><div className="detail-grid"><div><div className="kicker">What we do</div><p className="section-title" style={{fontSize:'clamp(38px,5vw,64px)',marginTop:18}}>Clarity at every stage.</p></div><div className="list">{sections.map(s=><div key={s.n}><span className="number">{s.n}</span><h3 style={{fontSize:26,margin:'10px 0'}}>{s.t}</h3>{s.items.map(i=><p key={i} style={{margin:'8px 0',color:'#666',lineHeight:1.5}}>— {i}</p>)}</div>)}</div></div><div style={{marginTop:80}}><Link className="btn dark" href="/contact">Discuss your project ↗</Link></div></div></section></main>}
